apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv
  namespace: mysql-ns
spec:
  storageClassName: standard
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath: 
    path: /mnt/data
  
